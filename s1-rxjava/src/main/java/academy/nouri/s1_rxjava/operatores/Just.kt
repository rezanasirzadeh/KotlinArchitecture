package ir.digireza.s1_rxjava.operatores

class Just {
    /*
     val numbers1: Array<Int> = arrayOf(98, 654, 74, 35, 854, 115, 223)
        val numbers2: Array<Int> = arrayOf(1, 8, 101, 204, 357)

        //Observable.just(1, 2, 5, 7, 65, 78)
        //Observable.just("Ali", "Maryam", "Mohesn", "Niloofar")
        Observable.just(numbers1, numbers2)
            .subscribeOn(Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe(object : Observer<Array<Int>> {
                override fun onSubscribe(d: Disposable) {
                    //Log.e(TAG, "Subscribed")
                    disposable = d
                }

                override fun onNext(t: Array<Int>) {
                    t.forEach {
                        Log.e(TAG, "$it ")
                    }
                }

                override fun onError(e: Throwable) {
                    Log.e(TAG, "Error ${e.message}")
                }

                override fun onComplete() {
                    Log.e(TAG, "Completed")
                }
            })
             */
}