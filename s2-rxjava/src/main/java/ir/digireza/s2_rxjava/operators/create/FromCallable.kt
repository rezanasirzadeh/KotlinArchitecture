package ir.digireza.s2_rxjava.operators.create

class FromCallable {
    /*
    *                 Observable.fromCallable(Callable {
                    return@Callable "Reza Nasirzadeh"
                }).subscribeOn(Schedulers.io())
                    .observeOn(AndroidSchedulers.mainThread())
                    .subscribe {
                        Log.e(Constants.TAG, it)
                    }

                Observable.fromCallable(Callable {
                    return@Callable 7
                })*/
}